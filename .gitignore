# ── Node / npm ─────────────────────────────────────
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json
# If you use pnpm or yarn lock files, keep them if you want deterministic builds
# pnpm-lock.yaml
# yarn.lock

# ── Build artefacts ─────────────────────────────────
# Front‑end (Vite) output
frontEnd/IAFP/dist/
# Back‑end (if you ever bundle)
backEnd/dist/

# ── Environment / secrets ────────────────────────
.env
.env.*
# Google service‑account credentials (never commit real keys)
google-credentials.json
# If you store credentials in a folder, ignore the whole folder as well
# credentials/
# credentials/*.json

# ── OS / editor artefacts ────────────────────────
.DS_Store
Thumbs.db
*.sublime-workspace
*.sublime-project
.vscode/
.idea/
*.iml

# ── Logs & temporary files ───────────────────────
logs/
*.log
*.tmp
tmp/
temp/

# ── Miscellaneous ────────────────────────────────
coverage/
*.tgz
*.bak
*.old
